models:
  - name: mart_moves_per_pokemon
    description: One row per pokemon x move
    columns:
      - name: pokemon_move_id
        data_type: VARCHAR
        description: '{{ doc("pokemon_move_id") }}'
        tests:
          - unique
          - not_null
      - name: poke_id
        data_type: INTEGER
        description: '{{ doc("poke_id") }}'
      - name: move_id
        data_type: INTEGER
        description: '{{ doc("move_id") }}'
sources: []
seeds: []
