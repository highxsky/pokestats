version: 2
models:
  - name: stg_past_stats
    columns:
      - name: fetch_date
        data_type: TIMESTAMP
        description: '{{ doc("fetch_date") }}'
        tests:
          - not_null
      - name: poke_id
        description: '{{ doc("poke_id") }}'
        data_type: INTEGER
        tests:
          - not_null
      - name: past_stats
        description: A pokemon's past stats
        data_type: JSON
sources: []
seeds: []
