version: 2
models:
  - name: stg_types
    columns:
      - name: fetch_date
        data_type: TIMESTAMP
        description: '{{ doc("fetch_date") }}'
        tests:
          - not_null
      - name: poke_id
        data_type: INTEGER
        tests:
          - not_null
        description: '{{ doc("poke_id") }}'
      - name: types
        data_type: JSON
        tests:
          - not_null
sources: []
seeds: []
